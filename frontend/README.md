# Frontend

The frontend documentation lives in the repository root:

**[Read the full project README](../README.md)**

## Client-only commands

```bash
npm install
npm run dev
npm run build
npm run lint
npm run preview
```

Set `VITE_API_URL` in `frontend/.env` to the backend URL before starting the app.
