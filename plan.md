1. user (normal user, admin user (set on database)) // Done
2. for normal user - user can register (Done), login  (Done), logout  (Done), change password, order items, manage orders, feedback, book table, payment
3. for admin user - user can register  (Done), login  (Done), logout  (Done), change password, manage users, add items, set prices, manage orders, manage categories, delete items
4. book table done
5. feedback done

// Signup, Login, Logout done, forgot password done

// Items add done

// Item add middleware done

// Item tags done

// Merge tags with items ! done

// Rating delete & update rating after delete any rating done

// Feedback form & Book table done

// Future task - Delete items (Done), update items (Done), image add, update & delete, delete image if item deleted